<%- include('partials/_header') %>
<nav class="nav-escudos">
    <div class="nav-left">
        <img src="https://s3.amazonaws.com/s3.timetoast.com/public/uploads/photos/12326115/LOGOTIPO_IPN.png" alt="EI">
        <h1>ProfESIME.com</h1>
    </div>
    <div>
        <img src="https://seeklogo.com/images/E/esime-logo-597242E938-seeklogo.com.png" alt="EE">
    </div>
</nav>   
<main>
        
        <% if(typeof(data) === 'undefined'){ %>
           
            <% }else{ %>
                <!--<pre><%= data %></pre>-->
                <% if(data == false){ %>
                        <script>alert('ERROR DATOS INCORRECTOS')</script>
                    <% }%>
           
        <% } %>
        
        <h2>Iniciar Sesión</h2>
        <form action="/ingresar" method="post">
            
            <label for="email">Correo institucional</label>
            <input type="email" id="email" name="email"required>
            <label for="password">Contraseña</label>
            <input type="password" id="password" name="password"required>
            <a href="/recuperarContra" class="forgot-password">Olvide mi contraseña</a>

            <div class="buttons">
                <button type="submit"  > Iniciar </button>
                <button type="button" onclick="location.href='/crearCuenta'" >Crear cuenta</button>
            </div>

        </form>
    </main>

    <%- include('partials/_footer') %>
